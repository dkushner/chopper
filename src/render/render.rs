use gfx;
use system::entity::Entity;
use std::collections::BTreeMap;
use nalgebra::Vector3;

pub trait RenderManager {
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn creation() {

    }
}
